a = """This is a multiline string"""
#strings are arrays so 
print(a[0])

#to loop through text
for x in "banana":
 print(x)
 #length of a string
 print(len(a))
 #slice strings
 b = "dog"
 print(b[1:2], b)

 # We also have string methods to
 b.upper or b.lower # just use b. to get em

 print("banana" in x) # returns True because a sequence with the value "banana" is in the list